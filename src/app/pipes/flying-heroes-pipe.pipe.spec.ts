import { FlyingHeroesPipePipe } from './flying-heroes-pipe.pipe';

describe('FlyingHeroesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyingHeroesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
